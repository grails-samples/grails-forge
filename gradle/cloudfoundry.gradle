apply plugin: 'cloudfoundry'

cloudfoundry {
    target = "https://api.run.pivotal.io"
    organization = "grails-org"
    application = "generator"
    space = "generator"
    file = file("build/libs/grails-forge-${version}.war")
    uri = 'start.grails.org'
}